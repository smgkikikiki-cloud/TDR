import { adminDb } from "@/lib/supabase";
import { saveEvent } from "@/app/admin/actions";
import { Field, Select, TextArea } from "@/components/admin/Fields";
export default async function NewEvent(){const db=adminDb(); const [b,m,p,c]=db?await Promise.all([db.from('brands').select('id,name_th').order('name_th'),db.from('models').select('id,name_th').order('name_th'),db.from('plants').select('id,name_th').order('name_th'),db.from('companies').select('id,name_th').order('name_th')]):[{data:[]},{data:[]},{data:[]},{data:[]}];return <div className="adminEditor"><div className="adminHeader"><div><small>EVENTS</small><h1>เพิ่มข่าว / Event</h1></div></div><form action={saveEvent} className="adminForm"><Field label="หัวข้อภาษาไทย" name="title_th" required/><Field label="หัวข้ออังกฤษ" name="title_en"/><Field label="วันที่เกิด Event" name="event_date" type="date" required/><Select label="ประเภท" name="event_type" defaultValue="news"><option value="news">News</option><option value="launch">Launch</option><option value="production">Production</option><option value="investment">Investment</option><option value="supplier">Supplier</option><option value="export">Export</option></Select><TextArea label="สรุป" name="summary_th"/><Field label="ชื่อแหล่งข้อมูล" name="source_name"/><Field label="Source URL" name="source_url" type="url"/><Select label="แบรนด์ที่เกี่ยวข้อง" name="related_brand_id"><option value="">—</option>{b.data?.map((x:any)=><option key={x.id} value={x.id}>{x.name_th}</option>)}</Select><Select label="รุ่นที่เกี่ยวข้อง" name="related_model_id"><option value="">—</option>{m.data?.map((x:any)=><option key={x.id} value={x.id}>{x.name_th}</option>)}</Select><Select label="โรงงานที่เกี่ยวข้อง" name="related_plant_id"><option value="">—</option>{p.data?.map((x:any)=><option key={x.id} value={x.id}>{x.name_th}</option>)}</Select><Select label="บริษัทที่เกี่ยวข้อง" name="related_company_id"><option value="">—</option>{c.data?.map((x:any)=><option key={x.id} value={x.id}>{x.name_th}</option>)}</Select><label className="adminCheckbox"><input type="checkbox" name="published" defaultChecked/><span>เผยแพร่บนเว็บ Public</span></label><div className="adminFormActions"><button className="adminPrimary">บันทึก Event</button></div></form></div>}
