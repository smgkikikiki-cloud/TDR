import { notFound } from "next/navigation"; import { adminDb } from "@/lib/supabase"; import { saveEvent } from "@/app/admin/actions"; import { Field,Select,TextArea } from "@/components/admin/Fields"; import { DeleteButton } from "@/components/admin/DeleteButton";
export default async function P({params}:{params:Promise<{id:string}>}){const{id}=await params;const db=adminDb();if(!db)notFound();const [row,b,m,p,c]=await Promise.all([db.from('events').select('*').eq('id',id).maybeSingle(),db.from('brands').select('id,name_th').order('name_th'),db.from('models').select('id,name_th').order('name_th'),db.from('plants').select('id,name_th').order('name_th'),db.from('companies').select('id,name_th').order('name_th')]);if(!row.data)notFound();const r=row.data;return <div className="adminEditor"><div className="adminHeader adminHeaderActions"><div><small>EVENTS / EDIT</small><h1>{r.title_th}</h1></div><DeleteButton kind="event" id={id}/></div><form action={saveEvent} className="adminForm"><input type="hidden" name="id" value={id}/><Field label="หัวข้อไทย" name="title_th" defaultValue={r.title_th} required/><Field label="หัวข้ออังกฤษ" name="title_en" defaultValue={r.title_en}/><Field label="วันที่" name="event_date" type="date" defaultValue={r.event_date} required/><Select label="ประเภท" name="event_type" defaultValue={r.event_type}><option value="news">News</option><option value="launch">Launch</option><option value="production">Production</option><option value="investment">Investment</option><option value="supplier">Supplier</option><option value="export">Export</option></Select><TextArea label="สรุป" name="summary_th" defaultValue={r.summary_th}/><Field label="แหล่งข้อมูล" name="source_name" defaultValue={r.source_name}/><Field label="Source URL" name="source_url" type="url" defaultValue={r.source_url}/><Select label="แบรนด์" name="related_brand_id" defaultValue={r.related_brand_id}><option value="">—</option>{b.data?.map((x:any)=><option key={x.id} value={x.id}>{x.name_th}</option>)}</Select><Select label="รุ่น" name="related_model_id" defaultValue={r.related_model_id}><option value="">—</option>{m.data?.map((x:any)=><option key={x.id} value={x.id}>{x.name_th}</option>)}</Select><Select label="โรงงาน" name="related_plant_id" defaultValue={r.related_plant_id}><option value="">—</option>{p.data?.map((x:any)=><option key={x.id} value={x.id}>{x.name_th}</option>)}</Select><Select label="บริษัท" name="related_company_id" defaultValue={r.related_company_id}><option value="">—</option>{c.data?.map((x:any)=><option key={x.id} value={x.id}>{x.name_th}</option>)}</Select><label className="adminCheckbox"><input type="checkbox" name="published" defaultChecked={r.published}/><span>เผยแพร่</span></label><div className="adminFormActions"><button className="adminPrimary">บันทึก</button></div></form></div>}
