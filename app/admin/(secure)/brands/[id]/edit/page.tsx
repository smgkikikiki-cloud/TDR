import {notFound} from"next/navigation";import{adminDb}from"@/lib/supabase";import{saveBrand}from"@/app/admin/actions";import{Field,Select,TextArea}from"@/components/admin/Fields";import{DeleteButton}from"@/components/admin/DeleteButton";
export default async function P({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<Record<string,string|undefined>>}){const{id}=await params;const sp=await searchParams;const db=adminDb();if(!db)notFound();const{data:r}=await db.from("brands").select("*").eq("id",id).maybeSingle();if(!r)notFound();return <div className="adminEditor"><div className="adminHeader adminHeaderActions"><div><small>BRANDS / EDIT</small><h1>{r.name_th}</h1>{sp.inuse?<p className="adminErrorText">ลบไม่ได้เพราะยังมี Model ผูกกับแบรนด์นี้</p>:null}</div><DeleteButton kind="brand" id={id} label="ลบแบรนด์"/></div><form action={saveBrand} className="adminForm"><input type="hidden" name="id" value={id}/><Field label="ชื่อแบรนด์" name="name_th" defaultValue={r.name_th} required/><Field label="ชื่ออังกฤษ" name="name_en" defaultValue={r.name_en}/><Field label="ประเทศต้นกำเนิด" name="country_origin" defaultValue={r.country_origin}/><Field label="Logo URL" name="logo_url" defaultValue={r.logo_url}/><Select label="สถานะ" name="status" defaultValue={r.status}><option value="active">Active</option><option value="inactive">Inactive</option></Select><TextArea label="หมายเหตุ" name="notes" defaultValue={r.notes}/><div className="adminFormActions"><button className="adminPrimary">บันทึก</button></div></form></div>}
