import Link from "next/link";import { getPlantsWithStats } from "@/lib/data";
export default async function Plants({searchParams}:{searchParams:Promise<{sort?:string}>}){const params=await searchParams;let rows:any[]=await getPlantsWithStats();const sort=params.sort||"capacity-desc";rows=[...rows].sort((a,b)=>sort==="models-desc"?(b.model_count-a.model_count):sort==="util-desc"?((b.utilization_estimate??-1)-(a.utilization_estimate??-1)):sort==="name"?a.name_th.localeCompare(b.name_th):(b.capacity_annual??-1)-(a.capacity_annual??-1));return <main className="plantsPage"><section className="industryHero"><small>PRODUCTION ASSETS</small><h1>โรงงานผลิตรถยนต์</h1><p>ดูโรงงานในฐานะโรงงาน — ขนาด, กำลังผลิต, utilization estimate, ที่ตั้ง และจำนวนรุ่นที่กำลังผลิต</p></section><form className="plantSort"><label>เรียงตาม <select name="sort" defaultValue={sort}><option value="capacity-desc">Capacity ใหญ่ → เล็ก</option><option value="models-desc">จำนวนรุ่นที่ผลิต</option><option value="util-desc">Utilization สูง → ต่ำ</option><option value="name">ชื่อโรงงาน</option></select></label><button>เรียง</button></form><div className="plantCardList">{rows.map((r:any)=><Link href={`/plants/${r.slug}`} className="plantSummaryCard" key={r.id}><div><small>{r.maker_group||"PLANT"}</small><h2>{r.name_th}</h2><p>{[r.province,r.district].filter(Boolean).join(" · ")||"Thailand"}</p></div><dl><div><dt>Capacity</dt><dd>{r.capacity_annual?r.capacity_annual.toLocaleString():"—"}</dd></div><div><dt>Utilization*</dt><dd>{r.utilization_estimate!=null?`${r.utilization_estimate}%`:"—"}</dd></div><div><dt>รุ่นที่ผลิต</dt><dd>{r.model_count}</dd></div></dl></Link>)}{!rows.length?<div className="publicEmptyCard">ยังไม่มีโรงงาน</div>:null}</div><p className="estimateFootnote">* Utilization เป็น estimate จากตัวเลข production ที่ TDR มีอยู่ ณ ช่วงเวลานั้น ไม่ใช่ตัวเลข audit ของโรงงาน</p></main>}
