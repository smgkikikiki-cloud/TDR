import Link from "next/link";import { PageIntro } from "@/components/PageIntro";import { getCompanies } from "@/lib/data";
export default async function Companies(){const rows=await getCompanies(250);return <><PageIntro eyebrow="COMPANIES" title="บริษัท" description="OEM ซัพพลายเออร์และบริษัทที่มีข้อมูลสาธารณะเชื่อมโยงกับอุตสาหกรรมยานยนต์ไทย"/><div className="tableShell"><table><thead><tr><th>บริษัท</th><th>ประเภท</th><th>บริษัทแม่</th><th>จังหวัด</th><th>สินค้า/บริการ</th></tr></thead><tbody>{rows.length?rows.map((r:any)=><tr key={r.id}><td><Link className="recordLink" href={`/companies/${r.slug}`}>{r.name_th}</Link></td><td>{r.company_type}</td><td>{r.parent_company||'—'}</td><td>{r.province||'—'}</td><td>{r.products_services||'—'}</td></tr>):<tr><td colSpan={5} className="publicEmpty">ยังไม่มีข้อมูลบริษัท</td></tr>}</tbody></table></div></>}
