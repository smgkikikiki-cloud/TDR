import Link from "next/link";import { getBrands } from "@/lib/data";
export default async function Brands(){const rows:any[]=await getBrands(250);return <main className="brandsDirectory"><section className="industryHero"><small>BRANDS</small><h1>แบรนด์รถในตลาดไทย</h1><p>เลือกแบรนด์เพื่อเปิดแคตตาล็อกรถทั้งหมดในฐานข้อมูล</p></section><div className="brandDirectoryGrid">{rows.length?rows.map((r:any)=><Link href={`/brands/${r.slug}`} className="brandDirectoryCard" key={r.id}><div>{r.logo_url?<img src={r.logo_url} alt={r.name_th}/>:<span>{r.name_th.slice(0,2).toUpperCase()}</span>}</div><b>{r.name_th}</b><small>{r.country_origin||""}</small></Link>):<div className="publicEmptyCard">ยังไม่มีข้อมูลแบรนด์</div>}</div></main>}
